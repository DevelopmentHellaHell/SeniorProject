export namespace Database {
    export enum Databases {
        USERS = "USERS",
        LOGS = "LOGS",
        NOTIFICATIONS = "NOTIFICATIONS"
    }

    export enum Tables {
        USER_ACCOUNTS = "USER"
        
    }
}